# WORKLOG — newest first
- 2026-08-04 · review-merge + follow-up · team review branch (feat/eval-independence, 3 commits) verified against the live local cluster — 58/58 including the new eval-independence guard and agent_runs migration — and merged to develop (--no-ff); executed the review's cheapest open item as an artifact not a paragraph: 0003_app_role.sql (recall_app: no DELETE, no DDL, runbooks read-only), grants verified via SHOW GRANTS, README "Access control" section added under The guarantee · next: still open from review — MCP on camera (needs creds), real-PIR track (~1.5h, cut-listed); and M0: Bedrock request (Titan V2 + Claude + Nova fallback) + Cloud cluster, tonight
- 2026-08-04 · review · rubric review pass (5 equally-weighted criteria, Devpost) found AC2 was scoring string identity — all 20 eval alert titles byte-identical to their target incident's title (1.00, 20/20), and search embeds title+description, so the benchmark could not fail. Fixed at the generator (ALERT_TITLES, monitor voice vs post-hoc human voice) + tests/test_eval_independence.py, whose real assertion is that no shared token is unique to the target inside the service-scoped search space; regenerated 20/20 -> 0/20, mean 0.32. Added agent_runs replayable decision log (0002, tools.log_step/run_log non-manifest so AC4 stays 4, agent.py still zero-SQL) + GET /runlog; log_step swallows by explicit ADR — telemetry must not fail a diagnosis. README repositioned off durability onto the structural-honesty guarantee (+ "What we don't claim"), docs/09 -> v2 with the refusal as its own beat at 0:28. PLAN.md written: 32h real capacity vs 41h planned, cut order pre-decided, kill gates per milestone. 47 fast tests green (58 with a cluster) · next: **M0 — Bedrock model access (Titan V2 + Claude + Nova fallback) and the Cloud cluster, tonight.** Four weeks on this same line; the Nova fallback exists so Aug 9 is a decision, not a wait
- 2026-08-04 · chore · branch migration executed: origin/develop (the stranded Aug 3 policy rewrite — docs/07, CI, wt.sh, start-work skill) merged into dev's 9 newer commits (3 trivial newest-first conflicts), `develop` now the integration line at the merged head, `dev` + permanent `test` deleted local and remote per the new model; repo = `main` (release-only) + `develop`; 50/50 green post-merge · next: unchanged — credentials, then the live half
- 2026-08-04 · docs · full doc re-verify pass: CLAUDE.md current-state + commands rewritten to the as-built reality (was still saying P0/probe-pending), docs/02 data-flow line 4 fixed (page polls GET /status, not the DB), README de-gimmicked for judges — five-W table removed, replaced with The problem (problem+vision merged) and a Who-it's-for section; branches promoted dev→test→main per docs/07 · next: unchanged — credentials
- 2026-08-04 · docs · doc pack synced to the as-built system: README rewritten (objective+vision up front, what/why/who/when/how table, honest built-vs-run-live phase status, local-node + chaos-rig quickstart, parked list = post-hackathon backlog), CHANGELOG Aug 4 entry (26→50 tests, credential gate is the only gate), C4 container edge fixed (status page → Lambda GET /status, was drawn polling the DB) + component text (GET read path, non-manifest helpers) · next: unchanged — creds, then Cloud probe → load → tune → AC2 eval → live e2e
- 2026-08-04 · P1–P3(credential-free, second slice) · everything buildable without creds now built: read path (`status_snapshot`/`health` in tools.py + GET /status·/health routing, AC12's `?memory=off` costs nothing server-side), status_page/index.html per docs/03 (tokens, elapsed clock, badge, memory panel, event log, ?api= config), docs/09 demo script v1 (~2:35 read), `infra/seed/load.py` (embed+upsert corpus, re-runnable), tests/retrieval_eval.py AC2 harness (skips with reason until corpus embedded), `make deploy` now bundles psycopg+pydantic+prompts (agent.py prompt path handles both layouts); 50/50 green · next: BLOCKED on creds — Cloud probe→migrate, Bedrock probe→dim marker, load.py, tune thresholds, AC2 eval, live e2e; then P3 is just filming-prep polish
- 2026-08-04 · P2+P4(credential-free slice) · agent loop real: `run_agent` Converse loop (manifest-only dispatch, TOOL_SPECS asserted == TOOL_MANIFEST, retrieval persisted by the loop not the model, tool validation errors fed back in-band so an invented ID never persists, MAX_TURNS loud-fail), prompts/system.md v1 (confidence-first, cite-or-stop, never write_incident during diagnosis), `ingest_handler` real (pydantic Alert, base64, 400s); AC7 rig live: infra/chaos 3-node compose v25.2.2 + `make chaos-up/down`, rehearsed — node 2 killed, survivors accepted a write, count intact; 45/45 green · next: creds land → `make probe` on Cloud, Bedrock probe → resolve dim marker, embed+load corpus, tune thresholds, AC2 eval; rig left stopped (CPU)
- 2026-08-04 · P2(probe-independent slice) · write surface real: `insert_incident` (idempotent on external_id + working_state seed row), `record_retrieval` (persists matches+confidence — what AC11 time-travels over), `propose_diagnosis` (validates every cited ID, empty citations legal only on the `confidence='none'` honesty branch), `write_incident` (scrub → embed → resolve); 0001_init applied to the local node — real DDL verified; 8 DB-backed tests vs live cockroach per docs/05 no-mocks rule (embed stubbed, it's not the SQL path); 34/34 green · next: agent.py Converse loop + prompts/system.md (Bedrock-blocked halves stay stubbed), or P1 embed+load the day creds land
- 2026-08-04 · P0 · local CRDB reinstalled (v25.2.2 → ~/.local/bin, on-demand start, no systemd; store ~/.local/share/recall-crdb) after the Aug 4 CPU-cleanup wipe; `make probe` passes locally with `feature.vector_index.enabled = true` — flag-then-works recorded in docs/02; DB strategy locked: Cloud primary + local single-node dev · next: human runs probe_runbook A–C on cockroachlabs.cloud + AWS (Basic-tier index, MCP service-account key, Bedrock request) → resolve the dim marker, finish P1 embed+load+eval
- 2026-08-03 · chore · branch model rewritten (docs/07): `dev`→`develop`, `main` becomes release-only + always tagged, permanent `test` branch retired in favour of short-lived `release/<name>` candidates, `feature/*`→`feat/*`, `spike/*`→`experiment/*`, new `test/` `docs/` `chore/` prefixes. Forced by a real git constraint: `refs/heads/test` blocks `refs/heads/test/*` (verified in a scratch repo). CI triggers, wt.sh, branches_init.sh, start-work skill, Makefile and README moved over in the same commit. `main` deliberately left frozen at `w1` · next: **P0 probe, human-gated**; docker rig queued
- 2026-08-02 · docs · architecture re-documented as a C4 set in `docs/diagrams/` (context/container/component, hand-authored SVG, house tokens from docs/03, presentation attributes not CSS so it rasterises for the video); Mermaid removed from README + docs/02; `CHANGELOG.md` created (Keep a Changelog, back-filled Jul 8 → Aug 2); README refreshed with real dates + honest per-phase status; hard rule 8 amended for asset-only folders · next: **P0 probe, human-gated**; local 3-node docker rig queued (image pulled, v26.2.5)
- 2026-08-02 · P1 · memory core, probe-independent half: `embed.py` (sole Bedrock embedding surface, D1 dims+normalize, throttle retry), `db.get_conn` + AC7 reconnect arm in `with_retry` (a killed node raises OperationalError, not SerializationFailure — the original snippet would have failed the node-kill take), `search_incidents` (service-scoped `<->`, decay re-rank, confidence bands) and `get_runbook` real; Match/SearchResult/Runbook pydantic models; SearchResult gained `runbook_ids` so the agent can reach a runbook without a 5th tool; 26/26 green (10 new pure-function tests) · next: **P0 probe, human-gated** — then embed+load the corpus, tune the two thresholds, run the AC2 eval
- 2026-08-02 · P0 · docs/08 final-sprint plan landed (Aug 2→18 calendar supersedes docs/01's dates; registered in CLAUDE.md + docs/index); D2 resolved in-file → Branch B psycopg hot path, MCP as dev/ops surface (docs/02, lambda/db.py); probe_bedrock.py now sends `dimensions:1024, normalize:true` and prints the L2 norm (D1); sleep-cycle struck from CLAUDE.md rule 6, submit dates corrected to Aug 17 ICT target / Aug 19 04:00 ICT wall; Aug 1 dev work (corpus, scrubber, AC2 pairs) promoted dev→test→main · next: **P0 probe, human-gated** — cluster + MCP service-account key + Bedrock access request; agent continues probe-independent P1 (db.get_conn, embed helper, search_incidents, get_runbook)
- 2026-07-08 · P1(pre-probe slice) · scrubber real (shared NAME_LIST) + deterministic corpus live: 80 postmortems/6 services, 12 runbooks, 20 paraphrase eval pairs pinned in tests/fixtures; 16/16 tests green (scrub zero-survivors, determinism, committed-file match, pair alignment) · next: P0 probe answers from human → resolve markers → schema apply + embed + load + retrieval_eval (AC2)
- 2026-07-08 · P0 · probe prep: container has no cloud creds and egress policy blocks the cockroach binary → live probe is human-side; research answered all four probe questions (v25.2 flag, Basic-tier index RISK, MCP service-account headless auth, Titan V2 1024-dim default) → scripts/probe_runbook.md with decision tree · next: human runs runbook A–C, reports answers → resolve markers, start P1 seed corpus
- 2026-07-08 · P0 · harness operationalized: .claude/ skills (/session-loop /verify /record /start-work /gate-check), SessionStart orient+bootstrap hook, Stop never-end-red hook, worker agent contract, permission allowlist — docs/06 spec → machinery · next: unchanged — run the P0 probe
- 2026-07-08 · P0 · judge-facing README v1 (badges, memory-layer table, why-CRDB table, roadmap) — AC6/AC9 groundwork pulled forward from P4 on request · next: unchanged — run the P0 probe
- 2026-07-08 · P0 · repo scaffolded from handoff pack (layout per docs/05, all indexes registered); branch/worktree model designed + tooled (docs/07, scripts/wt.sh, make branches-init); CI fast suite; structural tests green (AC4 manifest, module boundaries, retry) · next: run the P0 probe — create cluster, `make probe`, headless-MCP check, request Bedrock access
